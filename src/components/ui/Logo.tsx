import { Link } from 'gatsby';
import React from 'react'

type Props = {
}

const Logo: React.FC<Props> = ({}) => {
    return (
      <Link className="font-bold text-3xl" to="/">atramentoowa</Link>
    );
}
export default Logo